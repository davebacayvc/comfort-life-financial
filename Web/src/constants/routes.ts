const paths = {
  index: "/",
  home: "/home",
  services: "/services",
  portal: "/portal",
  contact: "/contact",
  about: "/about",
  invalid: "/invalid",
  events: "/events",
} as const;

export default paths;
