const paths = {
  index: "/",
  home: "/home",
  services: "/services",
  portal: "/portal",
  contact: "/contact",
  about: "/about",
  invalid: "/invalid",
  events: "/events",
  event_invites: "/events/invites/:id",
} as const;

export default paths;
